package com.generated.rescueStock.routes;

/** 移库单路由常量，controller 的 RequestMapping 与前端请求路径共用同一前缀。 */
public final class TransferOrderRoutes {
  public static final String PATH = "/api/transfer-order";
  public static final String RECEIVE = PATH + "/{id}/receive";

  private TransferOrderRoutes() {}
}
